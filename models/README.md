# Models

Drop `.glb` or `.gltf` files here to use them as 3D face models.

Supported formats:
- `.glb` (recommended)
- `.gltf`

Any model with morph targets (blendshapes) will work best for lip-sync animation.
